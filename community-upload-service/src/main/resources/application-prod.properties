spring.application.name=community-upload-service
spring.output.ansi.enabled=ALWAYS
server.port=8083

# Image upload
image.upload-dir=uploads
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=200MB
spring.mvc.favicon.enabled=false

# Eureka Client
eureka.client.serviceUrl.defaultZone=http://community-service-discovery:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Database
spring.datasource.url=jdbc:postgresql://community-database:5432/community_upload_service
spring.datasource.username=${DATABASE_USER:postgres}
spring.datasource.password=${DATABASE_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=update


